services:
  postgres:
    image: postgres:15
    container_name: auto_workshop_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: auto_workshop
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5433:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
    networks:
      - auto_network
    restart: unless-stopped

  pgadmin:
    image: dpage/pgadmin4
    container_name: auto_workshop_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - auto_network
    restart: unless-stopped

networks:
  auto_network:
    driver: bridge
